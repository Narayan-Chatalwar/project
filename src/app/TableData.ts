export interface TableData {
    id:number,
    name:string,
    position:string,
    contact:string,
    age:number,
    address:string,
    salary:number
}
export interface TableHeader {
    id:number,
    title:string
}